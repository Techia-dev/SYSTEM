# ============================================================
# Database — Prisma reads this directly
# ============================================================

# Format:
# postgresql://USER:PASSWORD@HOST:PORT/DATABASE?schema=public

DATABASE_URL="postgresql://postgres:admin123@localhost:5432/system?schema=public"

# Production (set these in Railway Dashboard, NOT in .env files):
# DATABASE_URL="postgresql://user:password@host:5432/database?schema=public&connection_limit=5"